
$HTTP["host"] == "talk.almatamagotchi.com" {
    server.document-root = "/usr/local/www/alma/talk"
    $HTTP["url"] =~ "^/api/" {
        proxy.server = ( "" => (( "host" => "127.0.0.1", "port" => 8092 )) )
    }
}
